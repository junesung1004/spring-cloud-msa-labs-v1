server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:///${user.dir}/backend/config-repo
#            - file://C:\sasac\spring-cloud-msa-labs-v1\backend\config-repo\
#            - .\backend\config-repo\
