server:
  port: 8888
spring:
  profiles:
    active: native
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations:
#            - file://C:\sasac\spring-cloud-msa-labs-v1\backend\config-repo\
            - .\backend\config-repo\